* Restructure into module
* Compare inputs to see if current and previous release are identical
* Use temporary git repository under .build folder
* Add support for PowerShell modules
* Fill out Maven support